# P0 验证记录

环境：rustc/cargo 1.98.1、protoc 36.1、bun 1.4.0、node 26.8.1；工具链位于仓库内
`.toolchain/`（gitignored，见 `scripts/bootstrap-toolchain.sh`）。

记录口径：只写实际执行并通过的结果；未实现能力明确标注，不把计划中的 "Expected" 复制成结果。

## Task 01 — 类型生成链

| 项 | 命令 | 结果 |
|---|---|---|
| Rust 标量向量 | `cargo test -p conex-proto --test scalars` | 3 passed / 0 failed |
| TS 标量向量 | `bun test sdk/typescript/tests/scalars.test.ts` | 3 passed / 0 failed |
| 生成一致性 | `cargo xtask generate --check` | up to date |

产物：`schema/generated/{conex.bin,rust/,jsonschema/}`、`sdk/typescript/src/generated/`。

边界说明：

- JSON Schema 由 `xtask/src/schema.rs` 从编译后的 FileDescriptorSet 确定性生成，**未使用**
  Go/protoschema 插件（环境无 Go）；生成器与产物版本记录在 `tools/codegen.lock.json`。
- 五个共享标量向量在 Rust 与 TS 两侧分类一致；`uint64` 仅接受规范十进制字符串并在两侧检查上界。
