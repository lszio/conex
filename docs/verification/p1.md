# P1 验证记录

> 状态：P1-01 契约冻结与 P1-02/03/06/07/08 离线切片已完成（2026-09-15/16）；2026-09-16 复核发现的规范化字节阻塞项已由 **P1-01b** 修正（见文末）。P1 各包**均未接入 wire/Registry**。后续工作包在执行完成时在此追加命令与结果。

环境：rustc/cargo 1.98.1、protoc 36.1、bun 1.4.0、node 26.8.1；工具链位于仓库内
`.toolchain/`（gitignored，见 `scripts/bootstrap-toolchain.sh`）。

记录口径：只写实际执行并通过的结果；未实现能力明确标注，不把计划中的 "Expected"
复制成结果。

## P1-01 — P1 契约冻结

| 项 | 命令 | 结果 |
|---|---|---|
| Rust 标量/wire/CID 向量 | `cargo test -p conex-proto` | scalars 3 + wire 7 + cid 4 passed |
| TS 标量/wire/CID 向量 | `bun test sdk/typescript/tests/{scalars,wire,cid}.test.ts` | 6 passed |
| Rust P1 分块向量 | `cargo test -p conex-proto --test chunking` | 3 passed |
| TS P1 分块向量 | `bun test sdk/typescript/tests/chunking.test.ts` | 2 passed |
| Rust P1 契约分类向量 | `cargo test -p conex-proto --test p1_contracts` | 4 passed |
| TS P1 契约分类向量 | `bun test sdk/typescript/tests/p1_contracts.test.ts` | 4 passed |
| 完整 P0 + P1 门禁 | `cargo xtask check` | all steps passed |

### 产出

- 新增 `schema/conex/v1/{chunking,session,stream,blob,operation}.proto`；protoc / prost / pbjson / ts-proto 全部生成成功，`schema/generated/` 与 `sdk/typescript/src/generated/` 一致（`cargo xtask generate --check` 通过）。
- 新增 `docs/contracts/p1-{chunking,blob,stream,operation}.md`，明确每条冻结决定 + 占位待定项。
- 新增 `conformance/vectors/p1/{chunking,blob,stream,operation}.json`。
- 新增 `crates/conex-proto/src/cid.rs::content_cid`，统一内容寻址函数（设计 §5.3）。
- 新增 `crates/conex-proto/tests/{chunking.rs,p1_contracts.rs}`；`sdk/typescript/tests/{chunking.test.ts,p1_contracts.test.ts}`。
- `xtask check` 增加 `conformance p1` 步骤；`tools/codegen.lock.json` 记录 P1 schema/vectors/contracts。
- `scripts/dev/p1-vectors.rs` 作为独立 Rust 助手，用于 cross-check chunking 黄金 CID。（已于 P1-01b 删除：它实现的是被取代的 `manifest/v1/` 文本编码，黄金向量改由 `conformance/tools/gen_manifest_goldens.py` 生成。）
- `docs/plans/2026-09-15-conex-p1.md` §3 拆出 §3.1–§3.9，给 P1-02..10 列出文件、接口、失败向量与命令。

### 已知偏差（待 P1-02..10 解决）

- 五份新 proto 的 typed input/output 严格解码（`MethodContract.prepare / validate_output`）尚未连入 MethodContract 注册路径；P1-03..10 在对应方法注册处接入。
- blob 持久后端的具体存储路径（`crates/conex-content`）与崩溃恢复日志格式属于 P1-06；本契约只规定 schema 与可观察行为。
- 流帧在 protobuf Profile 下的 wire 形态属于 P1-04。
- 一次性 ticket 与浏览器入口（OIDC code+PKCE）属于 P1-09。

## P1-06 — Blob 存储（demo slice）

| 项 | 命令 | 结果 |
|---|---|---|
| 单元测试（生命周期 + 崩溃 + GC） | `cargo test -p conex-content --test blob` | 11 passed |
| P0 + P1-01 + P1-06 完整门禁 | `cargo xtask check` | all steps passed |

### 产出

- 新增 `crates/conex-content/`：`store`（cap-std root、`LocalBlockStore`、原子 tmp+rename、`Recover` 扫描 refs/staging/pins 重建内存状态）、`content`（`ContentStore`/`Upload` façade）、`receipt`（`UploadState`/`CommitRecord`/`PinRecord`）、`error`（按设计 §5.2 的 ErrorCode 映射）。
- Layout：`blocks/<aa>/<bb>/<rest>`、`refs/<root>.{committed,blocks}`、`staging/<upload_id>/receipt.json`、`pins/<pin_id>.json`。所有写走 tmp+rename；崩溃恢复通过 `recover()` 完成，保证无「缺块却 committed」。
- 单元测试覆盖 `conformance/vectors/p1/blob.json` 全部八个 cases：raw / manifest commit、bad-blob 拒绝 + 已校验块保留、declared_root 冲突、缺块拒绝、持久性等级拒绝、chunk-cras-recover-after-commit、租约过期回收、pin/unpin、GC 不删 committed 块。
- `xtask conformance --vectors conformance/vectors/p1` 新增 `rust conex-content blob` 步骤。

### 已知偏差（待 P1-10 联合验收）

- `conex-content` 当前仅暴露 `ContentStore` API；`blob/put|chunk|commit|pin|unpin|have|get|cancel` MethodContract 注册 + MethodContract.prepare 严格解码在 P1-08 接入。

## P1-07 — Blob 流与续传（transfer layer）

| 项 | 命令 | 结果 |
|---|---|---|
| 单元测试（inline + 多块 + 1 GiB + 边界） | `cargo test -p conex-content --test transfer -- --test-threads=1` | 9 passed（含 1 GiB roundtrip，~95 s） |
| P0 + P1-01 + P1-06 + P1-07 完整门禁 | `cargo xtask check` | all steps passed |

### 产出

- 新增 `crates/conex-content/src/transfer.rs`：`chunk_payload` / `manifest_cid_for` / `decode_content` / `is_inline_eligible` / `blob_ref_for` / `inline_payload` / `MAX_BLOB_BYTES = 1 << 30`。
- `decode_content` 走 `std::io::Write` 而不是 `Vec<u8>`：1 GiB 内容在内存上始终有界（单块 ≤ 1 MiB + manifest 字节缓冲）。
- 测试覆盖 `conformance/vectors/p1/blob.json` 的「blob/get」「inline/ref」「1 GiB」三组：单块 raw、多块 manifest、declared_root 校验、缺块拒绝、尺寸不匹配拒绝、inline 阈值边界、BlobRef access/mime 字段、最大字节上限拒绝。

### 已知偏差（待 P1-10 / P1-04 接入）

- 传输层目前直接连 `ContentStore`；P1-04 落地后改走 `StreamFrame`（`sessionId / attachmentId / epoch / streamId / seq / message`）。
- inline 路径下「未上传单块资源」的 `blob/get` 走单文件 inline，与 multipart 的 manifest 路径在 `ContentAddress` oneof 里靠根类型区分；P1-10 联合验收补 ACK/credit 端到端。
- `PersistenceLevel` 的 broker 拒绝路径只在单元测试与 contract level 覆盖；完整 MethodContract 错误码返回在 P1-08 接入。

## P1-08 — 操作与条件写（demo slice）

| 项 | 命令 | 结果 |
|---|---|---|
| 单元测试（execution class → retry + dedup + state machine + crash recovery + GC） | `cargo test -p conex-core --test operation` | 16 passed |
| P0 + P1-01 + P1-06 + P1-07 + P1-08 完整门禁 | `cargo xtask check` | all steps passed |

### 产出

- 新增 `crates/conex-core/src/operation/mod.rs`：`ExecutionClass` 四类（ReadOnly / Idempotent / Deduplicated / NonReplayable）→ `RetryPolicy` 固定推导；`OperationStore` 本地持久（gitignored `operations/<id>.json` + `operations/<id>.result.json`），原子 tmp+rename；`OperationRecord` schema_version=1；`SettledResult` 含 success/failure/execution。
- 失败向量：同 key 不同 `param_digest` → conflict；同 key 同 `param_digest` 幂等；deduplicated 响应丢失 → operation/get 取回；non_replayable 已发送 → unknown 不自动重试；过期 → expired；崩溃恢复 + GC。
- 路径组件强制安全：`..` / `\\` / 空串拒绝；`/` 允许在 `resource_id`，由 `sanitize_component` 转换为 `_`。

### 已知偏差（待 P1-10 联合验收）

- `MethodContract` 注册 + `prepare` / `validate_output` 严格解码未连入 MethodContract 注册路径；P1-10 补 broker 适配。
- `OperationRecord` 走 in-process mutex；多 broker 进程的并发协调属于 P4（HA）。
- `expectedRevision` 条件写在 broker MethodContract 适配时连入（待 P1-10）。

## P1-03 — Session / attachment（demo slice）

| 项 | 命令 | 结果 |
|---|---|---|
| 单元测试（open/resume/renew/close + epoch fencing + crash recovery + GC） | `cargo test -p conex-core --test session` | 12 passed |
| P0 + P1-01 + P1-06 + P1-07 + P1-08 + P1-03 完整门禁 | `cargo xtask check` | all steps passed |

### 产出

- 新增 `crates/conex-core/src/session/mod.rs`：`SessionStore` + `SessionRecord`/`AttachmentRecord`/`SessionBinding`/`RecoveryLevel`；open 返回 sessionId + granted recovery；resume 按 attachment_epoch 原子 fencing（每次 +1）；renew 仅延长 lease；close 在最后一个 attachment 时删除整 session。
- 失败向量：`RecoveryLevel::Persistent` 在 P1 demo 强制降级为 `InProcess`（返回 UnsupportedRecovery，advertised 不一致直接拒）；binding 不匹配 → BindingMismatch；过期 → LeaseExpired → session_lost；path traversal 拒绝。
- Attachment 默认 lease 120 s（设计 §7.1），可调以测试 GC。

### 已知偏差（待 P1-02 WSS + P1-10 接入）

- `SessionBinding` 通过 MethodContract 的 Prepare 路径（连接认证 / hello/ready）派发；本 demo 仅提供本地状态机。
- 跨进程 Session 恢复（host 重启后复活）属于 P4；当前 P1 仅承诺窗口内网络重连。
- `session/resume` / `session/renew` / `session/close` MethodContract 注册 + WSS 帧绑定在 P1-02 / P1-10 接入。

## P1-02 — WSS 引导（demo slice，state machine）

| 项 | 命令 | 结果 |
|---|---|---|
| 单元测试（hello/ready 状态机 + 失败向量） | `cargo test -p conex-core --lib transport_ws` | 12 passed |
| P0 + P1-01 + P1-06 + P1-07 + P1-08 + P1-03 + P1-02 完整门禁 | `cargo xtask check` | all steps passed |

### 产出

- 新增 `crates/conex-core/src/transport_ws/mod.rs`：`ServerHandshake` + `ClientHandshake` 状态机，`AwaitingHello → AwaitingReady → Ready`；`conex/hello` → 服务端生成 `negotiationId` 一次性 → `conex/ready`；`HelloRequest`/`ReadyRequest` 使用 camelCase + prost `Plane` 走 `plane_serde` 自定义适配器（小写 wire / SCREAMING_SNAKE prost 反序列化）。
- 失败向量：业务帧早于 `ready` → `EarlyBusiness`；profile 不匹配 → `UnsupportedProfile`；plane 不匹配 → `PlaneMismatch`（`plane_mismatch`）；`negotiationId` 不匹配 → `NegotiationIdMismatch`；`conex/hello` 在 `AwaitingReady` 状态时再发 → `MalformedEnvelope`；`conex/ready` 在 `Ready` 之后 → `EarlyBusiness`；超 64 KiB → `BufferExceeded`；UnknownMethod；未指定 plane → `MissingPlane`。
- 两个 Profile（`conex-jsonrpc2-wss-v1`、`conex-protobuf-wss-v1`）都完成 hello/ready happy-path 验证。

### 已知偏差（待 P1-10 / 真实 WS 集成）

- 真实 WS frame I/O（axum/hyper 接入、握手超时取消、resume/超窗 reset 联动）属于 `conex-transport-ws`；本 demo slice 仅交付状态机。
- 业务帧的"协商编码切到 Profile 选定编码"由 transport crate 在 `conex/ready` result 后切换；本状态机仅放行，不再做内容解码。

## 复核 — 离线切片状态与阻塞项（2026-09-16）

范围：确认仓库实际状态与计划/验证记录一致；未新增实现。基线 `35482d0`（`refactor/arch`，工作区干净，与 `origin/refactor/arch` 一致）。

| 项 | 命令 | 结果 |
|---|---|---|
| 完整门禁 | `cargo xtask check` | all steps passed（271 s） |
| 门禁步骤清单 | 读 `xtask/src/check.rs` | fmt / clippy / cargo test / bun install / typecheck / bun test / generate --check / conformance / conformance p1；**无 e2e 步骤** |
| chunking 黄金根 CID 独立复算 | python（`hashlib.sha256` + CIDv1 raw 手算 base32，不引用本仓库代码） | 512 KiB `0x01` 的根 = `bafkreiha7xatdgk3b3vbttawrjbslxdgz5t2e3laqtxhywbdercr4nvkgy`，与 `chunking.json` 及 `conex-proto::cid::content_cid` 一致 |
| 跨 crate 根 CID 一致性 | 同上，比较 `manifest/v1/` 与 `manifest/v1` 两种前缀 | **不一致**：`conex-content::transfer::manifest_cid_for` 得 `bafkreidr75abqgsjqatzlbel4nvxdew6zbfuqxh65l7mctswgnmqmlii4` |
| `source/read` 对同一内容 | 读 `crates/conex-provider-fs/src/read.rs:97` | `cid_for_raw(整段字节)` = `bafkreibtufilz2ytyguzeiy6mw2pvulicuqs7qdm3naitfwtf3vsz346xm`，第三套地址 |

结论与缺口（记为待办，不视为通过）：

- 阻塞项 **P1-01b 规范化字节统一**（当时问题陈述与证据见计划 §2.1）；**已于同日修正**，见下文「P1-01b」。
- P1-02/03/06/07/08 仍为「离线实现 + 单元测试」，未接入 wire / Registry / MethodContract；各工作包既有的「已知偏差」条目继续有效。
- `crates/conex-proto/tests/p1_contracts.rs`、`sdk/typescript/tests/p1_contracts.test.ts` 只断言向量 JSON 有 `expect` 字段（形状），`stream.json` 无任何实现，因此「P1 契约分类向量 4 passed」不构成行为证据。
- `xtask/src/conformance.rs` 只用 `--vectors` 做目录存在性检查、步骤写死，`cargo xtask check` 因而把同一套 10 步跑两遍（`conex-content transfer` 的 1 GiB 用例各跑一次，实测 88.6 s）。
- 文档与实际不一致：`docs/verification/p0.md` Task 13、`2c9a06e` 提交信息、`.github/workflows/ci.yml` 均称门禁含 e2e；实际 `xtask check` 无该步骤，CI 也未单独跑 `cargo xtask e2e --suite p0-ts`。
- 已修正的状态表述：`README.md`、`docs/README.md`、路线图与 P1 计划头部（此前仍写「P1 待启动」）。

## P1-01b — 规范化字节统一（2026-09-16）

决策与冻结内容见计划 §2.1。规范化字节 = `ChunkManifest` 的 protobuf wire 编码；唯一实现 `crates/conex-proto/src/cid.rs`（Rust）与 `sdk/typescript/src/content.ts`（TS）。

| 项 | 命令 | 结果 |
|---|---|---|
| 独立实现逐字节比对 | `protoc --encode=conex.v1.ChunkManifest`（C++ protobuf，输入文本格式）对比 prost 与 ts-proto 输出 | 三者在 `two_full_chunks` 用例上给出同一 159 字节（hex 相同），根 CID `bafkreicib7rzn2qv2xfn25bl2hpj5v53k677jqdssoz4hx757w6lojjrvy` |
| Rust 标量/分块向量 | `cargo test -p conex-proto --lib --test chunking` | lib 4 passed + chunking 4 passed |
| TS 分块向量（生产模块） | `bun test sdk/typescript/tests/chunking.test.ts` | 3 passed（62 expect；含黄金字节 hex、分层 `leafManifestCids`、rejects） |
| TS 全量 | `bun test sdk/typescript/tests` | 23 passed, 1 skipped |
| 类型检查 | `bun run typecheck` | ok |
| blob 生命周期 | `cargo test -p conex-content --test blob` | 14 passed（新增 root-kind 矛盾、块长度篡改、缺块索引三类拒绝） |
| blob 传输 | `cargo test -p conex-content --test transfer -- --test-threads=1` | 9 passed（含 1 GiB 往返，83.7 s） |
| provider CID 对齐 | `cargo test -p conex-provider-fs -p conex-provider-http-catalog` | read 5 passed（含 256 KiB 上限边界）；catalog 5 + authorization 2 passed |
| 黄金向量可再生成 | `python3 conformance/tools/gen_manifest_goldens.py --check` | up to date |
| 生成产物一致 | `cargo xtask generate --check` | up to date（ts-proto 改为 `outputEncodeMethods=true`，生成物入库） |
| 完整门禁 | `cargo xtask check` | all steps passed（289.6 s，含新增的 `manifest goldens` 步骤） |
| 真实 SDK E2E | `cargo xtask e2e --suite p0-ts` | 1 passed |

### 产出

- `crates/conex-proto/src/cid.rs`：唯一内容寻址实现 — `manifest_tree` / `addressing_for_parts` / `content_cid_for_parts` / `content_cid` / `leaf_cids_for` / `leaf_length`；`MANIFEST_FANOUT = 1024` 分层规则；`MANIFEST_FORMAT_VERSION = 1`。
- `conformance/tools/gen_manifest_goldens.py`：独立黄金向量生成器（`protoc --encode` + Python 标准库；`--check` 已入门禁）。
- `conformance/vectors/p1/chunking.json`：重新生成 — `payload/sizeBytes/expectedRootKind/rootCid/rootManifestBytesLength/rootManifestBytesHex/leafManifestCids/leafCids` + 6 条 rejects。
- `conformance/vectors/p1/blob.json`：多块 commit 用例引用 `rootVector: two_full_chunks`；新增 root-kind 矛盾、块长度篡改、缺块索引三个用例。
- `crates/conex-content`：删除自有 `manifest_cid_for`/`cid_for_bytes`/`parse_cid`/`sha256_digest` 与 `manifest/v1` 前缀编码，统一使用 `conex_proto::cid`；`commit` 改为从 upload receipt 重建叶子（新增 `ReceivedChunk{index,cid}`、`leaves_in_order`），重算根并与 `declared_root` 比对，逐块校验逻辑长度，持久化 manifest 对象并把它们写入 `refs/<root>.blocks`；`refs/<root>.record.json` 保存 commit 记录（`recover()` 读取，不再伪造 `root_kind`/`committed_bytes`）。
- `sdk/typescript/src/content.ts`：TS 侧同一规则（`contentCid`/`manifestTree`/`leafCidsFor`/`parseContentCid`），依赖 `@bufbuild/protobuf`（ts-proto 编码器运行时，已加入 `dependencies`）；`tests/chunking.test.ts` 改为消费生产模块而非测试内副本。
- `crates/conex-provider-fs`、`crates/conex-provider-http-catalog`：`source/read` 的 `cid` 改用 `content_cid(bytes, CHUNK_SIZE)`。
- 删除 `scripts/dev/`（第三套已废弃的规范字节实现）与 `.gitignore` 中的 `scripts/dev/target/`。

### 仍未完成（不视为通过）

- §3.0 的 P1-01c：`xtask conformance` 仍未按 `--vectors` 选择步骤（同一套 10 步跑两遍）、`xtask check` 与 CI 仍未包含 e2e、`p1_contracts` 的形状型断言仍在。
- `ChunkManifest` 对象目前只在 commit 时派生并存储；经 wire 的 `blob/get` 取回 manifest 对象属 P1-07/P1-08。
- P1-02/03/06/07/08 仍未接入 wire/Registry/MethodContract（各工作包既有「已知偏差」条目继续有效）。

## 复核 — `a2ee453`「ship complete P1」与库级切片（2026-09-16）

> 提交信息：`feat(p1): ship complete P1 (broker dispatcher, agent registry, OIDC/ticket, gate honesty)`。本节不是对实现的验收，而是对真实状态的复核：哪些已经发生、哪些仍属空白。计划 §4 的复选框与本节一致——M1.2/M1.3/M1.5/M1.6 未勾是正确反映；文档头部的「已交付」不成立，已回滚为「P1 库级切片完成，未接入 host」。

| 项 | 命令 | 结果 |
|---|---|---|
| `cargo xtask check`（当前 `a2ee453`） | 完整 P0/P1 门禁 + e2e `p0-ts` | all steps passed（291.92 s） |
| `cargo test -p conex-host --test p1_e2e` | 4 用例 | blob_upload_round_trip / session_lifecycle_and_binding_mismatch / operation_dedup_round_trip / agent_registration_and_resolve 全部 ok（0.11 s，纯进程内） |
| 真实 host `conex-host host.toml` + curl | `/wss`、`/tickets`、`/oidc/authorize`、`/oidc/token` | **404**（无路由） |
| 真实 host + hello `bindingId` | `blob/put`、`agent/register`、`session/open` 经 `/rpc` | `-32003 unknown_provider`（broker/三个 store/HostSide 未挂入 `serve::build`） |
| 真实 host `conex/hello` | `provides` 字段 | 仅 `source/list`、`source/read`、`source/search`；`profileId` 仅 `conex-jsonrpc2-http-v1`。无任何 P1 profile/方法广告。 |
| `crates/conex-host/src/serve.rs` `build()` | 读 | 只构造 P0 `Host`/`BindingStore`/`StaticBearerAuth`；不构造 `ContentStore/SessionStore/OperationStore/HostSide/Broker` |
| `crates/conex-host/src/http.rs` `router` | 读 | 仅 `.route("/rpc", post(rpc))`；无 `/wss`、`/tickets`、`/oidc/*` |
| `crates/conex-host/src/config.rs` `HostConfig` | 读 | `deny_unknown_fields`，无 `content_root`/`session_root`/`operation_root` 字段；runbook 旧示例配置会被 TOML 拒绝（实测） |
| `crates/conex-host/src/broker.rs` `enforce_principal` | 读 289-310 | 恒真（`prepared_binding_for_claim` 永远 `Default`），是安慰剂 |
| `crates/conex-host/src/broker.rs` `operation_get`/`parse_dedup_key` | 读 707-758 | key 完全取自请求体；broker 层不与 caller 的 principal/tenant 比对 → 任意已认证主体可读他人 operation 记录 |
| `crates/conex-host/src/agent.rs` `agent/register` | 读 53-63 | 仅校验自报 `hostOrigin` 前缀 `conex://`；无 host 真实身份/公钥挑战 |
| `crates/conex-host/src/agent.rs` `TicketRegistry::issue` | 读 170-186 | ticket = `base64(SHA-256(principal:tenant:origin:host:issuedAtMs))`；无签名，可任意伪造 |
| `crates/conex-host/src/ws_transport.rs` `handle_connection` | 读 61-244 | 二进制帧非 UTF-8 时静默丢弃不响应（客户端挂起）；协商 10 s 超时未挂计时器 |
| `crates/conex-host/Cargo.toml` `tokio-tungstenite` | grep | 全仓无任何 `.rs` 使用（无 WS 集成测试） |
| `crates/conex-host/tests/p1_e2e.rs` 注释 | 读 1-7 | 旧注释引用 `p1_wss_smoke.rs`，文件不存在；已修正为「由 host 集成测试接管；未重新引入前不要加回引用」 |
| `conex-agent/src/main.rs` | 读 | `register`/`heartbeat`/`resolve` 全部只 `println!` 常量 JSON，不连 host |

### 与 `2c9a06e`（demo slices）相比新增

- `crates/conex-host/src/{broker,agent,ws_transport}.rs`（898 + 491 + 409 行）以及 `crates/conex-host/tests/p1_e2e.rs`（338 行）。
- `crates/conex-host/Cargo.toml`：增 `axum ws` feature + `tokio-tungstenite = "0.30"`；`tokio-tungstenite` 全仓无引用。
- `xtask/src/check.rs`：增 `end-to-end (real host + sdk)` 步骤（`cargo xtask e2e --suite p0-ts`），CI 因只跑 `xtask check` 自动覆盖。✅ P1‑01c 第 2 项。
- `xtask/src/conformance.rs`：未变，`--vectors` 仍只查目录存在，`check` 跑两遍。✗ P1‑01c 第 1 项。
- `crates/conex-proto/tests/p1_contracts.rs`、`sdk/typescript/tests/p1_contracts.test.ts`：未变，仍只断言形状；`stream.json` 无实现仍计入「通过」。✗ P1‑01c 第 3 项。

### 仍未完成（不视为通过）

- 主机接线：`HostConfig` 增 `content_root/session_root/operation_root`；`serve::build` 装 `ContentStore`/`SessionStore`/`OperationStore`/`HostSide`/`Broker`；注册 `/wss` `/tickets` `/oidc/*` 路由；`hello` 广告实际可达的能力。补一条真开 socket 的 host 集成测试替换空引用。
- 授权：`blob/*`/`session/*`/`operation/*`/`agent/*` 经 `StaticPolicy` + 审计；`operation/get` 的 key 由 caller 派生并与请求体比对；`agent/register` 用 host 真实身份校验替代自报 `hostOrigin` 前缀。
- P1‑04 Stream：`crates/conex-core/src/stream/{frame,ack,flow,reset,replay}.rs` + WSS 业务循环 + 有界控制队列 + `stream.json` 真行为断言。
- Protobuf WSS profile（`conex-protobuf-wss-v1` 当前 `not_implemented`）。
- 1 GiB 经 Stream 的弱网续传。
- 真 OIDC（issuer/audience 签名 + JWKS 缓存 + RS256 only）。
- `conformance --vectors` 真选步骤；`p1_contracts` 真行为断言；删 `session.json`/`transfer.json`/`agent.json`/`web_auth.json`/`wire-wss.json` 五个无消费者向量或补断言。
- 死码与调试输出清理（broker.rs 6 个 `_*_placeholder`、ws_transport.rs 2 个 `_unused_*`、`agent.rs:490 _unused_keep_duration`、`p1_e2e.rs:335 _unused`、`conex-agent/main.rs:89 _ensure_v1_used`、`broker.rs:187/231` `eprintln!`、`broker.rs:171` `let _ = (caller_principal, caller_tenant)`）。

