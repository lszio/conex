// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.3
//   protoc               v7.36.1
// source: conex/v1/stream.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "conex.v1";

/** 单方向流的元数据；在 stream/open 响应中返回，session/resume 也回填一份。 */
export interface StreamSpec {
  streamId?:
    | string
    | undefined;
  /** "data" 或 "control"；控制流（ack/flow/cancel/ping）独立有界。 */
  kind?:
    | string
    | undefined;
  /** 接收窗口字节（设计 §6.2）；窗口在流建立时固定，不能用后续 flow 扩展。 */
  windowBytes?:
    | string
    | undefined;
  /** 初始 maxFrameBytes。 */
  maxFrameBytes?: number | undefined;
}

/**
 * 数据帧（profile 决定字节形态，本消息是其语义字段）。注意：零字节 message
 * 禁止；ACK 必须是连续 seq 的最大已确认值；累计字节是 sender 实际发送字节。
 */
export interface StreamFrame {
  sessionId?: string | undefined;
  attachmentId?: string | undefined;
  epoch?: string | undefined;
  streamId?: string | undefined;
  seq?:
    | string
    | undefined;
  /** 单帧字节；由 Profile 编码（JSON/protobuf）后投到 wire。 */
  message?: Uint8Array | undefined;
}

/** stream/ack：累计已连续接收的最大 seq；不是窗口外字节通知。 */
export interface StreamAckRequest {
  sessionId?: string | undefined;
  attachmentId?: string | undefined;
  epoch?: string | undefined;
  streamId?: string | undefined;
  lastReceivedSeq?: string | undefined;
}

export interface StreamAckResponse {
  streamId?:
    | string
    | undefined;
  /**
   * 服务端按 (lastReceivedSeq+1) 起重新广告的窗口（<=协商窗口）；
   * 接收端不能基于此把窗口扩展到协商上限之外。
   */
  availableWindowBytes?: string | undefined;
}

/**
 * stream/flow：发送端报告累计已消费字节（receiver 视角），请求新窗口。
 * 重复或旧的 flow（consumedBytes <= 之前接受的最大值）忽略，不加信用。
 */
export interface StreamFlowRequest {
  sessionId?: string | undefined;
  attachmentId?: string | undefined;
  epoch?: string | undefined;
  streamId?: string | undefined;
  consumedBytes?:
    | string
    | undefined;
  /** 发送端请求新窗口字节数；不得超过协商上限且不得用于扩展。 */
  requestedWindowBytes?: string | undefined;
}

export interface StreamFlowResponse {
  newWindowBytes?:
    | string
    | undefined;
  /** 当 receiver 不能接受任何数据信用（控制仍然可发）。 */
  zeroWindow?: boolean | undefined;
}

/**
 * stream/reset：清空一段范围；接收端必须把未确认字节明确标为需重放，不能
 * 静默当作完整历史。
 */
export interface StreamResetRequest {
  sessionId?: string | undefined;
  attachmentId?: string | undefined;
  epoch?: string | undefined;
  streamId?: string | undefined;
  afterSeq?: string | undefined;
  reason?: string | undefined;
  resumeHandle?: string | undefined;
}

export interface StreamResetResponse {
  streamId?: string | undefined;
  acceptedAfterSeq?: string | undefined;
}

/** 慢消费者信号：control queue 满或 credit 耗尽 30 秒后由接收方发出。 */
export interface SlowConsumerSignal {
  streamId?: string | undefined;
  reason?: string | undefined;
  sinceMs?: string | undefined;
}

function createBaseStreamSpec(): StreamSpec {
  return { streamId: "", kind: "", windowBytes: "0", maxFrameBytes: 0 };
}

export const StreamSpec: MessageFns<StreamSpec> = {
  encode(message: StreamSpec, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.streamId !== undefined && message.streamId !== "") {
      writer.uint32(10).string(message.streamId);
    }
    if (message.kind !== undefined && message.kind !== "") {
      writer.uint32(18).string(message.kind);
    }
    if (message.windowBytes !== undefined && message.windowBytes !== "0") {
      writer.uint32(24).uint64(message.windowBytes);
    }
    if (message.maxFrameBytes !== undefined && message.maxFrameBytes !== 0) {
      writer.uint32(32).uint32(message.maxFrameBytes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamSpec {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseStreamSpec();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.streamId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.kind = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.windowBytes = reader.uint64().toString();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.maxFrameBytes = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): StreamSpec {
    return {
      streamId: isSet(object.streamId)
        ? globalThis.String(object.streamId)
        : isSet(object.stream_id)
        ? globalThis.String(object.stream_id)
        : "",
      kind: isSet(object.kind) ? globalThis.String(object.kind) : "",
      windowBytes: isSet(object.windowBytes)
        ? globalThis.String(object.windowBytes)
        : isSet(object.window_bytes)
        ? globalThis.String(object.window_bytes)
        : "0",
      maxFrameBytes: isSet(object.maxFrameBytes)
        ? globalThis.Number(object.maxFrameBytes)
        : isSet(object.max_frame_bytes)
        ? globalThis.Number(object.max_frame_bytes)
        : 0,
    };
  },

  toJSON(message: StreamSpec): unknown {
    const obj: any = {};
    if (message.streamId !== undefined && message.streamId !== "") {
      obj.streamId = message.streamId;
    }
    if (message.kind !== undefined && message.kind !== "") {
      obj.kind = message.kind;
    }
    if (message.windowBytes !== undefined && message.windowBytes !== "0") {
      obj.windowBytes = message.windowBytes;
    }
    if (message.maxFrameBytes !== undefined && message.maxFrameBytes !== 0) {
      obj.maxFrameBytes = Math.round(message.maxFrameBytes);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StreamSpec>, I>>(base?: I): StreamSpec {
    return StreamSpec.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StreamSpec>, I>>(object: I): StreamSpec {
    const message = createBaseStreamSpec();
    message.streamId = object.streamId ?? "";
    message.kind = object.kind ?? "";
    message.windowBytes = object.windowBytes ?? "0";
    message.maxFrameBytes = object.maxFrameBytes ?? 0;
    return message;
  },
};

function createBaseStreamFrame(): StreamFrame {
  return { sessionId: "", attachmentId: "", epoch: "0", streamId: "", seq: "0", message: new Uint8Array(0) };
}

export const StreamFrame: MessageFns<StreamFrame> = {
  encode(message: StreamFrame, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== undefined && message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.attachmentId !== undefined && message.attachmentId !== "") {
      writer.uint32(18).string(message.attachmentId);
    }
    if (message.epoch !== undefined && message.epoch !== "0") {
      writer.uint32(24).uint64(message.epoch);
    }
    if (message.streamId !== undefined && message.streamId !== "") {
      writer.uint32(34).string(message.streamId);
    }
    if (message.seq !== undefined && message.seq !== "0") {
      writer.uint32(40).uint64(message.seq);
    }
    if (message.message !== undefined && message.message.length !== 0) {
      writer.uint32(50).bytes(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamFrame {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseStreamFrame();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.sessionId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.attachmentId = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.epoch = reader.uint64().toString();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.streamId = reader.string();
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.seq = reader.uint64().toString();
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.message = reader.bytes();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): StreamFrame {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      attachmentId: isSet(object.attachmentId)
        ? globalThis.String(object.attachmentId)
        : isSet(object.attachment_id)
        ? globalThis.String(object.attachment_id)
        : "",
      epoch: isSet(object.epoch) ? globalThis.String(object.epoch) : "0",
      streamId: isSet(object.streamId)
        ? globalThis.String(object.streamId)
        : isSet(object.stream_id)
        ? globalThis.String(object.stream_id)
        : "",
      seq: isSet(object.seq) ? globalThis.String(object.seq) : "0",
      message: isSet(object.message) ? bytesFromBase64(object.message) : new Uint8Array(0),
    };
  },

  toJSON(message: StreamFrame): unknown {
    const obj: any = {};
    if (message.sessionId !== undefined && message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.attachmentId !== undefined && message.attachmentId !== "") {
      obj.attachmentId = message.attachmentId;
    }
    if (message.epoch !== undefined && message.epoch !== "0") {
      obj.epoch = message.epoch;
    }
    if (message.streamId !== undefined && message.streamId !== "") {
      obj.streamId = message.streamId;
    }
    if (message.seq !== undefined && message.seq !== "0") {
      obj.seq = message.seq;
    }
    if (message.message !== undefined && message.message.length !== 0) {
      obj.message = base64FromBytes(message.message);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StreamFrame>, I>>(base?: I): StreamFrame {
    return StreamFrame.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StreamFrame>, I>>(object: I): StreamFrame {
    const message = createBaseStreamFrame();
    message.sessionId = object.sessionId ?? "";
    message.attachmentId = object.attachmentId ?? "";
    message.epoch = object.epoch ?? "0";
    message.streamId = object.streamId ?? "";
    message.seq = object.seq ?? "0";
    message.message = object.message ?? new Uint8Array(0);
    return message;
  },
};

function createBaseStreamAckRequest(): StreamAckRequest {
  return { sessionId: "", attachmentId: "", epoch: "0", streamId: "", lastReceivedSeq: "0" };
}

export const StreamAckRequest: MessageFns<StreamAckRequest> = {
  encode(message: StreamAckRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== undefined && message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.attachmentId !== undefined && message.attachmentId !== "") {
      writer.uint32(18).string(message.attachmentId);
    }
    if (message.epoch !== undefined && message.epoch !== "0") {
      writer.uint32(24).uint64(message.epoch);
    }
    if (message.streamId !== undefined && message.streamId !== "") {
      writer.uint32(34).string(message.streamId);
    }
    if (message.lastReceivedSeq !== undefined && message.lastReceivedSeq !== "0") {
      writer.uint32(40).uint64(message.lastReceivedSeq);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamAckRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseStreamAckRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.sessionId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.attachmentId = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.epoch = reader.uint64().toString();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.streamId = reader.string();
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.lastReceivedSeq = reader.uint64().toString();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): StreamAckRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      attachmentId: isSet(object.attachmentId)
        ? globalThis.String(object.attachmentId)
        : isSet(object.attachment_id)
        ? globalThis.String(object.attachment_id)
        : "",
      epoch: isSet(object.epoch) ? globalThis.String(object.epoch) : "0",
      streamId: isSet(object.streamId)
        ? globalThis.String(object.streamId)
        : isSet(object.stream_id)
        ? globalThis.String(object.stream_id)
        : "",
      lastReceivedSeq: isSet(object.lastReceivedSeq)
        ? globalThis.String(object.lastReceivedSeq)
        : isSet(object.last_received_seq)
        ? globalThis.String(object.last_received_seq)
        : "0",
    };
  },

  toJSON(message: StreamAckRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== undefined && message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.attachmentId !== undefined && message.attachmentId !== "") {
      obj.attachmentId = message.attachmentId;
    }
    if (message.epoch !== undefined && message.epoch !== "0") {
      obj.epoch = message.epoch;
    }
    if (message.streamId !== undefined && message.streamId !== "") {
      obj.streamId = message.streamId;
    }
    if (message.lastReceivedSeq !== undefined && message.lastReceivedSeq !== "0") {
      obj.lastReceivedSeq = message.lastReceivedSeq;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StreamAckRequest>, I>>(base?: I): StreamAckRequest {
    return StreamAckRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StreamAckRequest>, I>>(object: I): StreamAckRequest {
    const message = createBaseStreamAckRequest();
    message.sessionId = object.sessionId ?? "";
    message.attachmentId = object.attachmentId ?? "";
    message.epoch = object.epoch ?? "0";
    message.streamId = object.streamId ?? "";
    message.lastReceivedSeq = object.lastReceivedSeq ?? "0";
    return message;
  },
};

function createBaseStreamAckResponse(): StreamAckResponse {
  return { streamId: "", availableWindowBytes: "0" };
}

export const StreamAckResponse: MessageFns<StreamAckResponse> = {
  encode(message: StreamAckResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.streamId !== undefined && message.streamId !== "") {
      writer.uint32(10).string(message.streamId);
    }
    if (message.availableWindowBytes !== undefined && message.availableWindowBytes !== "0") {
      writer.uint32(16).uint64(message.availableWindowBytes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamAckResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseStreamAckResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.streamId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.availableWindowBytes = reader.uint64().toString();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): StreamAckResponse {
    return {
      streamId: isSet(object.streamId)
        ? globalThis.String(object.streamId)
        : isSet(object.stream_id)
        ? globalThis.String(object.stream_id)
        : "",
      availableWindowBytes: isSet(object.availableWindowBytes)
        ? globalThis.String(object.availableWindowBytes)
        : isSet(object.available_window_bytes)
        ? globalThis.String(object.available_window_bytes)
        : "0",
    };
  },

  toJSON(message: StreamAckResponse): unknown {
    const obj: any = {};
    if (message.streamId !== undefined && message.streamId !== "") {
      obj.streamId = message.streamId;
    }
    if (message.availableWindowBytes !== undefined && message.availableWindowBytes !== "0") {
      obj.availableWindowBytes = message.availableWindowBytes;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StreamAckResponse>, I>>(base?: I): StreamAckResponse {
    return StreamAckResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StreamAckResponse>, I>>(object: I): StreamAckResponse {
    const message = createBaseStreamAckResponse();
    message.streamId = object.streamId ?? "";
    message.availableWindowBytes = object.availableWindowBytes ?? "0";
    return message;
  },
};

function createBaseStreamFlowRequest(): StreamFlowRequest {
  return { sessionId: "", attachmentId: "", epoch: "0", streamId: "", consumedBytes: "0", requestedWindowBytes: "0" };
}

export const StreamFlowRequest: MessageFns<StreamFlowRequest> = {
  encode(message: StreamFlowRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== undefined && message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.attachmentId !== undefined && message.attachmentId !== "") {
      writer.uint32(18).string(message.attachmentId);
    }
    if (message.epoch !== undefined && message.epoch !== "0") {
      writer.uint32(24).uint64(message.epoch);
    }
    if (message.streamId !== undefined && message.streamId !== "") {
      writer.uint32(34).string(message.streamId);
    }
    if (message.consumedBytes !== undefined && message.consumedBytes !== "0") {
      writer.uint32(40).uint64(message.consumedBytes);
    }
    if (message.requestedWindowBytes !== undefined && message.requestedWindowBytes !== "0") {
      writer.uint32(48).uint64(message.requestedWindowBytes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamFlowRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseStreamFlowRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.sessionId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.attachmentId = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.epoch = reader.uint64().toString();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.streamId = reader.string();
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.consumedBytes = reader.uint64().toString();
            continue;
          }
          case 6: {
            if (tag !== 48) {
              break;
            }

            message.requestedWindowBytes = reader.uint64().toString();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): StreamFlowRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      attachmentId: isSet(object.attachmentId)
        ? globalThis.String(object.attachmentId)
        : isSet(object.attachment_id)
        ? globalThis.String(object.attachment_id)
        : "",
      epoch: isSet(object.epoch) ? globalThis.String(object.epoch) : "0",
      streamId: isSet(object.streamId)
        ? globalThis.String(object.streamId)
        : isSet(object.stream_id)
        ? globalThis.String(object.stream_id)
        : "",
      consumedBytes: isSet(object.consumedBytes)
        ? globalThis.String(object.consumedBytes)
        : isSet(object.consumed_bytes)
        ? globalThis.String(object.consumed_bytes)
        : "0",
      requestedWindowBytes: isSet(object.requestedWindowBytes)
        ? globalThis.String(object.requestedWindowBytes)
        : isSet(object.requested_window_bytes)
        ? globalThis.String(object.requested_window_bytes)
        : "0",
    };
  },

  toJSON(message: StreamFlowRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== undefined && message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.attachmentId !== undefined && message.attachmentId !== "") {
      obj.attachmentId = message.attachmentId;
    }
    if (message.epoch !== undefined && message.epoch !== "0") {
      obj.epoch = message.epoch;
    }
    if (message.streamId !== undefined && message.streamId !== "") {
      obj.streamId = message.streamId;
    }
    if (message.consumedBytes !== undefined && message.consumedBytes !== "0") {
      obj.consumedBytes = message.consumedBytes;
    }
    if (message.requestedWindowBytes !== undefined && message.requestedWindowBytes !== "0") {
      obj.requestedWindowBytes = message.requestedWindowBytes;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StreamFlowRequest>, I>>(base?: I): StreamFlowRequest {
    return StreamFlowRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StreamFlowRequest>, I>>(object: I): StreamFlowRequest {
    const message = createBaseStreamFlowRequest();
    message.sessionId = object.sessionId ?? "";
    message.attachmentId = object.attachmentId ?? "";
    message.epoch = object.epoch ?? "0";
    message.streamId = object.streamId ?? "";
    message.consumedBytes = object.consumedBytes ?? "0";
    message.requestedWindowBytes = object.requestedWindowBytes ?? "0";
    return message;
  },
};

function createBaseStreamFlowResponse(): StreamFlowResponse {
  return { newWindowBytes: "0", zeroWindow: false };
}

export const StreamFlowResponse: MessageFns<StreamFlowResponse> = {
  encode(message: StreamFlowResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.newWindowBytes !== undefined && message.newWindowBytes !== "0") {
      writer.uint32(8).uint64(message.newWindowBytes);
    }
    if (message.zeroWindow !== undefined && message.zeroWindow !== false) {
      writer.uint32(16).bool(message.zeroWindow);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamFlowResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseStreamFlowResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.newWindowBytes = reader.uint64().toString();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.zeroWindow = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): StreamFlowResponse {
    return {
      newWindowBytes: isSet(object.newWindowBytes)
        ? globalThis.String(object.newWindowBytes)
        : isSet(object.new_window_bytes)
        ? globalThis.String(object.new_window_bytes)
        : "0",
      zeroWindow: isSet(object.zeroWindow)
        ? globalThis.Boolean(object.zeroWindow)
        : isSet(object.zero_window)
        ? globalThis.Boolean(object.zero_window)
        : false,
    };
  },

  toJSON(message: StreamFlowResponse): unknown {
    const obj: any = {};
    if (message.newWindowBytes !== undefined && message.newWindowBytes !== "0") {
      obj.newWindowBytes = message.newWindowBytes;
    }
    if (message.zeroWindow !== undefined && message.zeroWindow !== false) {
      obj.zeroWindow = message.zeroWindow;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StreamFlowResponse>, I>>(base?: I): StreamFlowResponse {
    return StreamFlowResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StreamFlowResponse>, I>>(object: I): StreamFlowResponse {
    const message = createBaseStreamFlowResponse();
    message.newWindowBytes = object.newWindowBytes ?? "0";
    message.zeroWindow = object.zeroWindow ?? false;
    return message;
  },
};

function createBaseStreamResetRequest(): StreamResetRequest {
  return {
    sessionId: "",
    attachmentId: "",
    epoch: "0",
    streamId: "",
    afterSeq: "0",
    reason: "",
    resumeHandle: undefined,
  };
}

export const StreamResetRequest: MessageFns<StreamResetRequest> = {
  encode(message: StreamResetRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== undefined && message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.attachmentId !== undefined && message.attachmentId !== "") {
      writer.uint32(18).string(message.attachmentId);
    }
    if (message.epoch !== undefined && message.epoch !== "0") {
      writer.uint32(24).uint64(message.epoch);
    }
    if (message.streamId !== undefined && message.streamId !== "") {
      writer.uint32(34).string(message.streamId);
    }
    if (message.afterSeq !== undefined && message.afterSeq !== "0") {
      writer.uint32(40).uint64(message.afterSeq);
    }
    if (message.reason !== undefined && message.reason !== "") {
      writer.uint32(50).string(message.reason);
    }
    if (message.resumeHandle !== undefined) {
      writer.uint32(58).string(message.resumeHandle);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamResetRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseStreamResetRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.sessionId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.attachmentId = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.epoch = reader.uint64().toString();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.streamId = reader.string();
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.afterSeq = reader.uint64().toString();
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.reason = reader.string();
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.resumeHandle = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): StreamResetRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      attachmentId: isSet(object.attachmentId)
        ? globalThis.String(object.attachmentId)
        : isSet(object.attachment_id)
        ? globalThis.String(object.attachment_id)
        : "",
      epoch: isSet(object.epoch) ? globalThis.String(object.epoch) : "0",
      streamId: isSet(object.streamId)
        ? globalThis.String(object.streamId)
        : isSet(object.stream_id)
        ? globalThis.String(object.stream_id)
        : "",
      afterSeq: isSet(object.afterSeq)
        ? globalThis.String(object.afterSeq)
        : isSet(object.after_seq)
        ? globalThis.String(object.after_seq)
        : "0",
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      resumeHandle: isSet(object.resumeHandle)
        ? globalThis.String(object.resumeHandle)
        : isSet(object.resume_handle)
        ? globalThis.String(object.resume_handle)
        : undefined,
    };
  },

  toJSON(message: StreamResetRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== undefined && message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.attachmentId !== undefined && message.attachmentId !== "") {
      obj.attachmentId = message.attachmentId;
    }
    if (message.epoch !== undefined && message.epoch !== "0") {
      obj.epoch = message.epoch;
    }
    if (message.streamId !== undefined && message.streamId !== "") {
      obj.streamId = message.streamId;
    }
    if (message.afterSeq !== undefined && message.afterSeq !== "0") {
      obj.afterSeq = message.afterSeq;
    }
    if (message.reason !== undefined && message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.resumeHandle !== undefined) {
      obj.resumeHandle = message.resumeHandle;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StreamResetRequest>, I>>(base?: I): StreamResetRequest {
    return StreamResetRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StreamResetRequest>, I>>(object: I): StreamResetRequest {
    const message = createBaseStreamResetRequest();
    message.sessionId = object.sessionId ?? "";
    message.attachmentId = object.attachmentId ?? "";
    message.epoch = object.epoch ?? "0";
    message.streamId = object.streamId ?? "";
    message.afterSeq = object.afterSeq ?? "0";
    message.reason = object.reason ?? "";
    message.resumeHandle = object.resumeHandle ?? undefined;
    return message;
  },
};

function createBaseStreamResetResponse(): StreamResetResponse {
  return { streamId: "", acceptedAfterSeq: "0" };
}

export const StreamResetResponse: MessageFns<StreamResetResponse> = {
  encode(message: StreamResetResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.streamId !== undefined && message.streamId !== "") {
      writer.uint32(10).string(message.streamId);
    }
    if (message.acceptedAfterSeq !== undefined && message.acceptedAfterSeq !== "0") {
      writer.uint32(16).uint64(message.acceptedAfterSeq);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamResetResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseStreamResetResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.streamId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.acceptedAfterSeq = reader.uint64().toString();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): StreamResetResponse {
    return {
      streamId: isSet(object.streamId)
        ? globalThis.String(object.streamId)
        : isSet(object.stream_id)
        ? globalThis.String(object.stream_id)
        : "",
      acceptedAfterSeq: isSet(object.acceptedAfterSeq)
        ? globalThis.String(object.acceptedAfterSeq)
        : isSet(object.accepted_after_seq)
        ? globalThis.String(object.accepted_after_seq)
        : "0",
    };
  },

  toJSON(message: StreamResetResponse): unknown {
    const obj: any = {};
    if (message.streamId !== undefined && message.streamId !== "") {
      obj.streamId = message.streamId;
    }
    if (message.acceptedAfterSeq !== undefined && message.acceptedAfterSeq !== "0") {
      obj.acceptedAfterSeq = message.acceptedAfterSeq;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StreamResetResponse>, I>>(base?: I): StreamResetResponse {
    return StreamResetResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StreamResetResponse>, I>>(object: I): StreamResetResponse {
    const message = createBaseStreamResetResponse();
    message.streamId = object.streamId ?? "";
    message.acceptedAfterSeq = object.acceptedAfterSeq ?? "0";
    return message;
  },
};

function createBaseSlowConsumerSignal(): SlowConsumerSignal {
  return { streamId: "", reason: "", sinceMs: "0" };
}

export const SlowConsumerSignal: MessageFns<SlowConsumerSignal> = {
  encode(message: SlowConsumerSignal, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.streamId !== undefined && message.streamId !== "") {
      writer.uint32(10).string(message.streamId);
    }
    if (message.reason !== undefined && message.reason !== "") {
      writer.uint32(18).string(message.reason);
    }
    if (message.sinceMs !== undefined && message.sinceMs !== "0") {
      writer.uint32(24).uint64(message.sinceMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SlowConsumerSignal {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseSlowConsumerSignal();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.streamId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.reason = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.sinceMs = reader.uint64().toString();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): SlowConsumerSignal {
    return {
      streamId: isSet(object.streamId)
        ? globalThis.String(object.streamId)
        : isSet(object.stream_id)
        ? globalThis.String(object.stream_id)
        : "",
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      sinceMs: isSet(object.sinceMs)
        ? globalThis.String(object.sinceMs)
        : isSet(object.since_ms)
        ? globalThis.String(object.since_ms)
        : "0",
    };
  },

  toJSON(message: SlowConsumerSignal): unknown {
    const obj: any = {};
    if (message.streamId !== undefined && message.streamId !== "") {
      obj.streamId = message.streamId;
    }
    if (message.reason !== undefined && message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.sinceMs !== undefined && message.sinceMs !== "0") {
      obj.sinceMs = message.sinceMs;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SlowConsumerSignal>, I>>(base?: I): SlowConsumerSignal {
    return SlowConsumerSignal.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SlowConsumerSignal>, I>>(object: I): SlowConsumerSignal {
    const message = createBaseSlowConsumerSignal();
    message.streamId = object.streamId ?? "";
    message.reason = object.reason ?? "";
    message.sinceMs = object.sinceMs ?? "0";
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
